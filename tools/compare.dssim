#!/bin/bash

# Utility script for comparing two images using dssim (https://github.com/kornelski/dssim)
# from within vidsim

dssim "$1" "$2" | awk '{score = $1 > 1 ? 0 : 1 - $1; printf("%.4f\n", score);}'
