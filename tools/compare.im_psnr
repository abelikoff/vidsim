#!/bin/bash

# Utility script for comparing two images using ImageMagick's compare w/ PSNR score
# from within vidsim.

compare -metric PSNR "$1" "$2" /dev/null 2>&1 | awk -F '[()]' '{ printf("%.4f\n", $2); }'
