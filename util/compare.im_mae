#!/bin/bash

# Utility script for comparing two images using ImageMagick's compare w/ MAE score
# from within vidsim.

compare -metric MAE "$1" "$2" /dev/null 2>&1 | awk -F '[()]' '{ printf("%.4f\n", $2); }'
